name: flutter_stories
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  storybook_flutter: ^0.10.0+1


dev_dependencies:
  flutter_test:
    sdk: flutter
  mews_pedantic: ^0.4.1-dev.1

flutter:
  uses-material-design: true
